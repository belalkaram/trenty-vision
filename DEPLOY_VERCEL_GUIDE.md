# 🚀 دليل نشر نظام Trenty Vision WhatsApp CRM على Vercel (بدون Baileys)

هذا الدليل يوضح بالخطوات التفصيلية كيفية نشر واجهة النظام ولوحة التحكم (Frontend SPA) ومحرك الـ REST API الكامل على منصة **Vercel** مجاناً وبسرعة فائقة.

---

## 💡 كيف يعمل النظام على Vercel؟

1. **الواجهة الأمامية (Frontend SPA):**
   - تم بناؤها باستخدام **React 19 + Vite + Tailwind CSS**.
   - تُنشر على شبكة **Vercel Edge CDN** العالمية لتقديم سرعة تحميل فائقة في مختلف البلدان.

2. **الواجهة الخلفية (Backend REST API):**
   - تعمل بنظام **Serverless Functions** عبر النقطة `api/index.ts` التي تستخدم **Fastify**.
   - تدير جميع عمليات المصادقة، الموظفين، المحطات، القواعد، أوقات العمل، المحادثات، جهات الاتصال، وسجلات التدقيق.

3. **ملاحظة هامة بشأن Baileys (واتساب ويب):**
   - بيئة **Vercel Serverless** هي بيئة عديمة الحالة (Stateless) وتتوقف تلقائياً عند عدم وجود طلبات لتوفير الموارد، وبالتالي **لا تناسب مقابس الاتصال الدائمة (Persistent WebSockets)** الخاصة بـ Baileys.
   - تم تصميم النسخة لتشغيل الـ CRM الكامل وإدارة المحادثات على Vercel بسلاسة. في حال رغبت لاحقاً بتشغيل محرك استقبال وإرسال واتساب الحي عبر Baileys، يمكنك تشغيل سيرفر Baileys كخدمة خلفية مستقلة (Worker) على خادم دائم مثل VPS أو Railway أو Render وربطه بنفس قاعدة بياناتك المشتركة.

---

## 🛠️ المتطلبات السابقة

1. **حساب على GitHub:** لحفظ ورفع الكود المصدري.
2. **حساب على Vercel:** (مجاني عبر [vercel.com](https://vercel.com)).
3. **قاعدة بيانات PostgreSQL سحابية:** ننصح باستخدام **Supabase** (مجاني) أو **Neon.tech**.

---

## 🗄️ الخطوة 1: تجهيز قاعدة البيانات السحابية (Supabase)

1. توجه إلى [Supabase](https://supabase.com) وأنشئ حساباً جديداً ثم أنشئ مشروعاً جديداً (New Project).
2. من لوحة تحكم Supabase، اذهب إلى:
   `Project Settings` ⬅️ `Database` ⬅️ مرر لأسفل حتى **Connection string** ⬅️ اختر تبويب **URI**.
3. انسخ رابط الاتصال الذي يشبه:
   ```text
   postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?sslmode=require
   ```
   *(تأكد من استبدال `[YOUR-PASSWORD]` بكلمة المرور التي اخترتها للمشروع).*

4. **تشغيل الجداول والبيانات الأولية (Migrations & Seed):**
   من جهازك المحلي في مجلد المشروع، ضع رابط قاعدة بياناتك الجديدة مؤقتاً في ملف `.env` أو شغّل:
   ```bash
   # تطبيق هيكل الجداول
   npm run db:migrate

   # إدخال البيانات الافتراضية وحساب المدير الأول
   npm run db:seed
   ```
   > **بيانات المدير الافتراضية المسجلة:**
   > - البريد الإلكتروني: `admin@trenty.com`
   > - كلمة المرور: `Admin@123456`

---

## 📦 الخطوة 2: رفع المشروع إلى مستودع GitHub

من داخل مجلد المشروع `e:\Whatsapp crm`:

```bash
git add .
git commit -m "feat: setup vercel deployment and enhanced features"
git branch -M main
git push origin main
```

---

## 🌐 الخطوة 3: استيراد المشروع في Vercel

1. ادخل إلى لوحة تحكم **[Vercel Dashboard](https://vercel.com/dashboard)**.
2. اضغط على **Add New...** ثم اختر **Project**.
3. اختر مستودع الـ GitHub الخاص بك واضغط **Import**.
4. في شاشة إعدادات المشروع (Configure Project):
   - **Project Name:** اختر اسماً لمشروعك (مثال: `trenty-vision-crm`).
   - **Framework Preset:** اختر **Vite** (أو اتركه تلقائياً).
   - **Root Directory:** اتركه `./`.
   - **Build and Output Settings:** (تلقائياً يتم قراءتها من `vercel.json`):
     - *Build Command:* `npm run build:client`
     - *Output Directory:* `dist/client`

---

## 🔑 الخطوة 4: ضبط المتغيرات البيئية (Environment Variables)

في شاشة الاستيراد بـ Vercel، افتح قسم **Environment Variables** وأضف القيم التالية:

| المتغير | القيمة المطلوبة / مثال | الوصف |
| :--- | :--- | :--- |
| `NODE_ENV` | `production` | نمط التشغيل الإنتاجي |
| `DATABASE_URL` | رابط قاعدة بيانات Supabase الذي نسخته | رابط الاتصال بقاعدة البيانات |
| `JWT_ACCESS_SECRET` | نص عشوائي قوي لا يقل عن 32 حرفاً | مفتاح توقيع توكن الجلسة |
| `JWT_REFRESH_SECRET` | نص عشوائي قوي لا يقل عن 32 حرفاً | مفتاح تجديد التوكن |
| `COOKIE_SECRET` | نص عشوائي لا يقل عن 32 حرفاً | لتشفير ملفات تعريف الارتباط |
| `ENCRYPTION_KEY` | `0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef` | مفتاح تشفير hex بطول 64 حرفاً بالضبط |
| `DEFAULT_COMPANY_NAME`| `Trenty Vision` | اسم المنشأة الافتراضي |
| `DEFAULT_TIMEZONE` | `Asia/Kuwait` | التوقيت الرسمي الافتراضي |

> **💡 نصيحة لتوليد مفاتيح عشوائية آمنة فوراً:**
> يمكنك استخدام الأمر التالي في الـ Terminal:
> ```bash
> node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
> ```

---

## 🚀 الخطوة 5: بدء النشر والاختبار (Deploy)

1. اضغط على زر **Deploy**.
2. سيقوم Vercel خلال 1-2 دقيقة ببناء واجهة الـ React وتهيئة دوال الـ Serverless API.
3. بمجرد انتهاء النشر، ستحصل على رابط رسمي مثل:
   `https://trenty-vision-crm.vercel.app`
4. افتح الرابط، وسجّل الدخول بحساب المشرف:
   - **البريد:** `admin@trenty.com`
   - **كلمة المرور:** `Admin@123456`

---

## ✨ الميزات الجاهزة على الرابط المنشور

1. **📱 تجربة متكاملة على الموبايل والشاشات الكبيرة:**
   - إمكانية فتح أي محادثة وتصفحها كاملة على الموبايل مع زر رجوع سريع `>` لقائمة الدردشات.
   - إظهار أرقام الهواتف وشارات الموظفين والمحطات في كل مكان.
2. **👤 بطاقة العميل كنافذة منبثقة تفاعلية:**
   - فتح بيانات العميل في Modal حديث يتيح تعديل الاسم، إعادة التوجيه للموظفين/المحطات، الملاحظات الداخلية، وخط زمني للمتابعات (Timeline).
3. **⏰ التحكم الكامل في الرد خارج أوقات العمل:**
   - جدول مواعيد العمل الرسمية بالساعة والدقائق.
   - تحديد أيام العمل الأسبوعية.
   - زر تفعيل/إيقاف صريح يضمن عدم إرسال الرسالة نهائياً عند إيقافها.
